# Automatically load environment variables from .env.loadstress
# direnv will auto-load this when you cd into the directory

# Load the loadstress environment
dotenv .env.loadstress

# Also activate Python virtual environment if it exists
if [ -d ".venv" ]; then
  source .venv/bin/activate
fi
